rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite que qualquer usuário autenticado (incluindo o anônimo)
    // leia e escreva dados.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}